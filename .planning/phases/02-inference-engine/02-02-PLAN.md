---
phase: 02-inference-engine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/services/inference_service.py, src/core/optimizer.py, requirements.txt]
autonomous: true
requirements: [ENGINE-02]
must_haves:
  truths:
    - "Model can be exported to ONNX format"
    - "Inference latency is measured and logged (<3s target)"
    - "TensorRT engine generation logic exists for NVIDIA hardware"
  artifacts:
    - path: "src/core/optimizer.py"
      provides: "ONNX and TensorRT conversion scripting"
---

<objective>
Optimize the inference engine for edge deployment (TensorRT).

Purpose: Meet the strict clinical latency requirement of ≤3 seconds on NVIDIA hardware while maintaining accuracy.
Output: Optimization scripts for ONNX conversion and TensorRT engine generation.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-inference-engine/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ONNX Export Logic</name>
  <files>src/core/optimizer.py, requirements.txt</files>
  <read_first>src/services/inference_service.py</read_first>
  <action>Create a script in src/core/optimizer.py to export the EfficientNet-B4 model to ONNX. Add onnx, onnxruntime to requirements.txt. Handle dynamic vs fixed batch sizing (fixed batch 1 for Jetson).</action>
  <verify>python src/core/optimizer.py export -> check if model.onnx exists</verify>
  <acceptance_criteria>
    - optimizer.py contains torch.onnx.export
    - requirements.txt includes onnx
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: TensorRT Engine Generation Scripting</name>
  <files>src/core/optimizer.py</files>
  <action>Add logic for building a TensorRT engine from the ONNX model using the TensorRT Python API (or subprocess call to `trtexec`). Configure FP16 precision for Jetson optimization.</action>
  <verify>Verify trtexec command generation in optimizer.py; if on NVIDIA, verify actual .engine generation</verify>
  <acceptance_criteria>
    - optimizer.py generates the correct trtexec command for FP16 optimization
    - support for INT8 calibration (placeholder for v2)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Setup Benchmarking Utility</name>
  <files>src/services/inference_service.py</files>
  <action>Enhance InferenceService to: 1. Measure inference time in ms, 2. Log latency to stdout/system-log, 3. provide a warm-up phase (v1 optimization best practice).</action>
  <verify>Inference calls log "[Inference] Latency: 245ms"</verify>
  <acceptance_criteria>
    - latency logging is integrated into predict()
    - model version and hardware accelerator (CPU/GPU/TRT) are reported
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] ONNX export script works consistently
- [ ] Optimization commands are generated with correct TRT version flags
- [ ] Latency is visible in system logs for clinical audit
</verification>

<success_criteria>
- All tasks completed
- Model is ready for hardware-accelerated deployment
- Benchmarking shows actual performance metrics for Phase 2 sign-off
</success_criteria>

<output>
After completion, create `.planning/phases/02-inference-engine/02-02-SUMMARY.md`
</output>
