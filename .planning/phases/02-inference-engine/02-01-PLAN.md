---
phase: 02-inference-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/services/inference_service.py, src/core/config.py, requirements.txt]
autonomous: true
requirements: [ENGINE-01, ENGINE-03, ENGINE-04]
must_haves:
  truths:
    - "System outputs 4-class probability distribution for every Case"
    - "Referral flag is correctly set based on P(class>=2) > threshold"
    - "Inference model version is logged with every result"
  artifacts:
    - path: "src/services/inference_service.py"
      provides: "Model loading and inference wrapper"
      exports: ["InferenceService"]
---

<objective>
Implement the core AI inference service for OralGuard triage.

Purpose: Provide the deep learning backbone for the system, classifying oral images into 4 diagnostic tiers.
Output: Inference service using EfficientNet-B4 (v2) with statistical fallback/mock for dev environments.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/01-ingestion-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Inference Service with EfficientNet Backbone</name>
  <files>src/services/inference_service.py, requirements.txt, src/core/config.py</files>
  <action>Create InferenceService class. Implement loading EfficientNet-B4 (placeholder or torchvision). Define 4 classes: Normal, Benign, Pre-malignant, Malignant. Add torch, torchvision, and timm to requirements.txt. Include MODEL_VERSION in src/core/config.py.</action>
  <verify>tsc/pytest check proving InferenceService can be instantiated and has a predict method</verify>
  <acceptance_criteria>
    - inference_service.py contains "class InferenceService"
    - initialization loads a model (or mock if no GPU/weights)
    - requirements.txt updated with ML libraries
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement 4-tier Triage Logic and Referral Flags</name>
  <files>src/services/inference_service.py</files>
  <read_first>src/core/config.py</read_first>
  <action>Implement predict() method returning a dict of probabilities. Implement check_referral() using SUM(P(Pre-malignant), P(Malignant)) > settings.REFERRAL_THRESHOLD (0.35). Ensure softmax is applied to raw logits.</action>
  <verify>Small script passing a dummy tensor to predict() and checking the keys/threshold trigger</verify>
  <acceptance_criteria>
    - predict() returns probabilities for 4 classes
    - logic for referral triggers correctly when probabilities cross 0.35
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Connect Inference with Ingestion Case Flow</name>
  <files>src/api/ingestion.py</files>
  <read_first>src/services/inference_service.py, src/models/cases.py</read_first>
  <action>Update upload_image to: 1. Pass enhanced image to InferenceService, 2. Retrieve probabilities and referral status, 3. Update Case record in DB with prediction_class and confidence scores, 4. Return results in API response.</action>
  <verify>Post image -> verify API response includes "prediction": "class name" and "referral": true/false</verify>
  <acceptance_criteria>
    - ingestion.py calls "inference_service.predict"
    - Case model in DB is updated with triage results
    - Response contains 4-class distribution
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] EfficientNet-B4 model loads (or initializes mock in non-ML env)
- [ ] Triage results are recorded in the encrypted case database
- [ ] Referral flag correctly identifies simulated high-risk inputs
</verification>

<success_criteria>
- All tasks completed
- AI backbone is integrated into the clinical workflow
- No noticeable increase in upload latency for v1 (using CPU/Mock)
</success_criteria>

<output>
After completion, create `.planning/phases/02-inference-engine/02-01-SUMMARY.md`
</output>
