---
phase: 01-ingestion-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/db/database.py, src/models/cases.py, src/services/storage_service.py, requirements.txt]
autonomous: true
requirements: [INGEST-04]
must_haves:
  truths:
    - "Image metadata (id, timestamp, blur_score) is stored in SQLite"
    - "Database file is encrypted (verify via 'strings output.db' shows no plaintext headers)"
    - "Raw and enhanced images are saved to local filesystem in .oral_data/ directory"
  artifacts:
    - path: "src/db/database.py"
      provides: "Encrypted SQLite connection"
      contains: "PRAGMA key"
    - path: "src/services/storage_service.py"
      provides: "Local file persistence"
---

<objective>
Implement encrypted local storage for clinical data and images.

Purpose: Ensure medical data is stored securely at-rest on the edge device, even without internet connectivity.
Output: Encrypted SQLite database layer and local file storage service.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ingestion-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Encrypted SQLite (SQLCipher) Layer</name>
  <files>src/db/database.py, requirements.txt</files>
  <action>Configure SQLAlchemy with SQLCipher. Set up "PRAGMA key" using a secure key from environment variables (fallback to dev-key for v1). Add "pysqlcipher3" (or relevant node/python equivalent) to requirements.txt. Create get_db session dependency.</action>
  <verify>Database file exists and cannot be opened with standard sqlite3 CLI without key</verify>
  <acceptance_criteria>
    - database.py contains encrypted connection logic
    - uses "PRAGMA key"
    - requirements.txt updated
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Define Case Models and Storage Service</name>
  <files>src/models/cases.py, src/services/storage_service.py</files>
  <read_first>src/db/database.py</read_first>
  <action>Create Case model with fields: id, patient_id, timestamp, blur_score, status, raw_path, enhanced_path. Create StorageService to save binary files to .oral_data/ with UUID filenames.</action>
  <verify>tsc/pytest check on models and storage logic saving a dummy file</verify>
  <acceptance_criteria>
    - cases.py contains Case(Base) model
    - storage_service.py uses os/pathlib to save files
    - .oral_data directory is created automatically if missing
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Connect Ingestion with Storage</name>
  <files>src/api/ingestion.py</files>
  <read_first>src/services/storage_service.py, src/models/cases.py</read_first>
  <action>Update upload_image endpoint to: 1. Save raw image via StorageService, 2. Save enhanced image, 3. Create Case record in DB with scores and paths, 4. Return the new Case ID to the caller.</action>
  <verify>Post image -> verify file exists in .oral_data/ -> verify row exists in encrypted DB</verify>
  <acceptance_criteria>
    - ingestion.py calls storage_service.save
    - ingestion.py creates DB record
    - returns "case_id"
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Database is unreadable by standard SQLite browsers
- [ ] Files are stored in .oral_data/ with non-identifiable names
- [ ] End-to-end flow from POST to DB success works
</verification>

<success_criteria>
- All tasks completed
- Clinical data is encrypted at-rest
- Filesystem is used for binary storage to keep DB small
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-foundation/01-02-SUMMARY.md`
</output>
