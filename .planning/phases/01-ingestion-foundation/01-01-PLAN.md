---
phase: 01-ingestion-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/api/ingestion.py, src/services/image_processor.py, src/core/config.py]
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03]
must_haves:
  truths:
    - "User can upload JPEG/PNG images to /api/v1/ingest"
    - "System returns 'quality_fail' if image is blurry (Laplacian < 50)"
    - "System returns enhanced image data (CLAHE applied)"
  artifacts:
    - path: "src/services/image_processor.py"
      provides: "CLAHE and Blur detection logic"
      contains: "cv2.createCLAHE"
    - path: "src/api/ingestion.py"
      provides: "Ingestion endpoints"
      exports: ["ingest_image"]
---

<objective>
Implement the core image ingestion and processing pipeline.

Purpose: Provide the entry point for clinical images and ensure they meet diagnostic quality standards before storage.
Output: FastAPI ingestion endpoint and OpenCV-based image processor service.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup FastAPI Project and Ingestion Endpoint</name>
  <files>src/main.py, src/api/ingestion.py, src/core/config.py, requirements.txt</files>
  <action>Initialize a FastAPI application. Create a POST /api/v1/ingest/upload endpoint that accepts a multipart/form-data image (File). Define basic configuration in src/core/config.py for Laplacian threshold (default 50). Add fastapi, pydantic, and python-multipart to requirements.txt.</action>
  <verify>curl -X POST -F "file=@test.jpg" http://localhost:8000/api/v1/ingest/upload returns 200</verify>
  <acceptance_criteria>
    - src/api/ingestion.py contains "router = APIRouter()" and "async def upload_image"
    - src/main.py includes the ingestion router
    - requirements.txt contains "fastapi"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Image Processing Service (CLAHE and Blur)</name>
  <files>src/services/image_processor.py, requirements.txt</files>
  <read_first>src/core/config.py</read_first>
  <action>Create ImageProcessor class. Implement detect_blur using cv2.Laplacian(img, cv2.CV_64F).var(). Implement apply_clahe using cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8,8)). Add opencv-python-headless to requirements.txt.</action>
  <verify>pytest or a script demonstrating Laplacian variance calculation and CLAHE application on a test image</verify>
  <acceptance_criteria>
    - image_processor.py contains "def detect_blur" and "def apply_clahe"
    - uses "cv2.createCLAHE(clipLimit=2.0)"
    - requirements.txt contains "opencv-python-headless"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate Processor with Ingestion Endpoint</name>
  <files>src/api/ingestion.py</files>
  <read_first>src/services/image_processor.py</read_first>
  <action>Update upload_image endpoint to: 1. Read binary, 2. Convert to CV2 image, 3. Check blur, 4. Apply CLAHE if pass, 5. Return success result with enhancement stats or failure message if blurry.</action>
  <verify>curl -X POST -F "file=@blurry.jpg" ... returns 400/error; curl -X POST -F "file=@sharp.jpg" ... returns 200 with result</verify>
  <acceptance_criteria>
    - ingestion.py calls "processor.detect_blur"
    - ingestion.py calls "processor.apply_clahe"
    - returns JSON with "quality": "pass/fail"
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] FastAPI server starts without errors
- [ ] Image upload endpoint handles JPEG/PNG
- [ ] Blur detection correctly identifies high/low variance
- [ ] CLAHE noticeably improves contrast on sample mucosal images
</verification>

<success_criteria>
- All tasks completed
- Image ingestion pipeline is functional and quality-aware
- No secrets or PII leaked in logs
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-foundation/01-01-SUMMARY.md`
</output>
