---
phase: 03-explainability-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/services/inference_service.py, src/services/storage_service.py]
autonomous: true
requirements: [EXPLAIN-01, EXPLAIN-03]
must_haves:
  truths:
    - "System generates a heatmap overlay (.png) for non-Normal predictions"
    - "Heatmap is saved to .oral_data/heatmaps/ and path is stored in Case"
  artifacts:
    - path: "src/services/inference_service.py"
      provides: "Grad-CAM generation logic"
      contains: "register_forward_hook"
---

<objective>
Implement Grad-CAM (Class Activation Mapping) to visualize diagnostic evidence.

Purpose: Increase clinician confidence by showing the specific regions of a mucosal image the model focused on for its prediction.
Output: Heatmap generation service integrated into the inference workflow.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-inference-engine/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Grad-CAM Hooks in InferenceService</name>
  <files>src/services/inference_service.py</files>
  <action>Add _register_hooks and generate_heatmap methods to InferenceService. Identify target layer (EfficientNet features[8] or equivalent). Capture feature maps and gradients during backward pass on the winning class.</action>
  <verify>Small test script proving that generate_heatmap returns a non-empty numpy array of the original image size</verify>
  <acceptance_criteria>
    - inference_service.py contains "def generate_heatmap"
    - uses valid PyTorch hooks (register_forward_hook/register_full_backward_hook)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Heatmap Rendering and Overlay Logic</name>
  <files>src/services/image_processor.py, src/services/inference_service.py</files>
  <action>Create overlay_heatmap method in ImageProcessor. Use cv2.applyColorMap (JET/VIRIDIS) on normalized heatmap. Combine with original RGB image (alpha=0.6). Return final rendered BGR image.</action>
  <verify>Output image saved and visually checked to ensure heatmap is properly scaled/aligned</verify>
  <acceptance_criteria>
    - image_processor.py contains "def overlay_heatmap"
    - output is a 3-channel BGR image
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate Heatmap with Storage and API</name>
  <files>src/api/ingestion.py, src/services/storage_service.py, src/models/cases.py</files>
  <read_first>src/services/inference_service.py, src/services/storage_service.py</read_first>
  <action>Update storage_service to support a "heatmaps" category. Update upload_image to: 1. Generate heatmap if prediction is NOT "Normal", 2. Save heatmap via StorageService, 3. Update Case record with heatmap_path.</action>
  <verify>POST high-risk image -> verify "heatmap_url" is returned in API and file exists in .oral_data/heatmaps/</verify>
  <acceptance_criteria>
    - storage_service handles "heatmaps" subdir
    - ingestion API returns heatmap path/ID
    - Case model has heatmap_path field
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Heatmap generation completes in <500ms
- [ ] Overlays show clear spatial focus on simulated dental lesions
- [ ] Normal cases avoid redundant heatmap generation unless flags are set
</verification>

<success_criteria>
- All tasks completed
- Explainable AI (XAI) feature functional
- No memory leaks from un-cleared hooks
</success_criteria>

<output>
After completion, create `.planning/phases/03-explainability-safety/03-01-SUMMARY.md`
</output>
