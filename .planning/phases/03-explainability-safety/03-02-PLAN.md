---
phase: 03-explainability-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/services/inference_service.py, src/models/cases.py, src/api/ingestion.py]
autonomous: true
requirements: [EXPLAIN-02, EXPLAIN-04]
must_haves:
  truths:
    - "System calculates an uncertainty score (variance) for every inference"
    - "Uncertainty score is stored in the database for each Case"
    - "Triage results can be flagged as 'Uncertain' if variance exceeds threshold"
  artifacts:
    - path: "src/services/inference_service.py"
      provides: "MC Dropout logic"
      contains: "torch.stack"
---

<objective>
Implement MC Dropout for uncertainty quantification.

Purpose: Quantify model "confusion" to prevent overconfident misdiagnosis, specifically for edge-case samples in rural settings.
Output: Uncertainty scoring system integrated into the Triage results.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-inference-engine/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MC Dropout Forward Passes</name>
  <files>src/services/inference_service.py</files>
  <action>Implement predict_with_uncertainty method. Perform 10 forward passes with dropout enabled (using torch.stack or similar). Calculate mean softmax and variance across passes. (Mock fallback: randomized variance).</action>
  <verify>predict() returns "uncertainty" in probabilities dict</verify>
  <acceptance_criteria>
    - inference_service.py contains "def predict_with_uncertainty"
    - uses 10 passes by default
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Store Uncertainty Metrics in Database</name>
  <files>src/models/cases.py, src/db/database.py</files>
  <action>Update Case model to include uncertainty (Float) and confidence_distribution (JSON/String). Implement JSON serialization for the 4-tier distribution.</action>
  <verify>Verify DB schema has new columns using sqlite3 CLI</verify>
  <acceptance_criteria>
    - Case model includes uncertainty and distribution fields
    - SQLAlchemy handles JSON serialization correctly for distribution
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate Uncertainty with Referral Flag</name>
  <files>src/api/ingestion.py</files>
  <read_first>src/services/inference_service.py</read_first>
  <action>Update upload_image to: 1. Use predict_with_uncertainty() results, 2. Store metrics in DB, 3. Trigger "referral" even if P<0.35 IF uncertainty > threshold (safety overwrite).</action>
  <verify>Post "confused" mock image -> verify referral flag triggers via uncertainty path</verify>
  <acceptance_criteria>
    - triage response includes "uncertainty": X
    - referral logic incorporates uncertainty threshold
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] 10 forward passes complete within ≤1s (total latency remains <3s)
- [ ] Confusing inputs (simulated) show measurably higher uncertainty than clear ones
- [ ] DB records show full confidence distribution for clinical audit
</verification>

<success_criteria>
- All tasks completed
- Predictive uncertainty is quantified and stored
- Safety fail-safe (uncertainty referral) functional
</success_criteria>

<output>
After completion, create `.planning/phases/03-explainability-safety/03-02-SUMMARY.md`
</output>
