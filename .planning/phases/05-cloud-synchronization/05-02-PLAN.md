# Plan: 05-02 Referral Notification Simulation (Mock)

## Requirements Covered
- **SYNC-03**: Support "Referral Up" notification.
- **TELE-READY**: v1 clinical bridge verification.

## Goals
Implement a **CloudMock** router that simulates the OralGuard Central Receiver for verification.

## Dependencies (Wave 0)
- `SyncService` (05-01-PLAN)

## Tasks (Wave 1)

1. **CloudMock Receiver (FastAPI)**
    - Create `src/api/mock_cloud.py`.
    - Implement `POST /cloud-sink/cases` to receive clinical bundles.
    - Log received bundles: "Referral Up - Case {id} - {triage_class}".
2. **Referral Acknowledgment**
    - `CloudMock` should return a `remote_sync_id`.
    - Local `Case` model should store this `remote_sync_id`.
3. **End-to-End Triage Simulation**
    - Implement a verification script `tests/simulate_screening.py`.
    - Flow: Upload (Ingest) -> Inference -> Report -> Sync (Mocked).
    - Assert: Case status becomes `SYNCED`.

## Verification Instructions
- **End-to-End Check**: Run `simulate_screening.py` and verify all phase 1-5 features are triggered.
- **Audit Check**: Verify images and reports are present in both local storage and mock cloud receiver.

---
*Created: 2026-03-23*
