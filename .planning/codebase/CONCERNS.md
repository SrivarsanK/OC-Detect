# Technical Concerns

## Greenfield
- **Project Goal:** No PROJECT.MD yet. Requires initialized spec.
- **Spec Drift:** Need to keep .planning/ up to date with actual code as it evolves.

## Environment
- **Windows / PowerShell:** Permissions issues resolved by using `cmd /C`.
- **Environment Isolation:** Ensure agent skills and metadata are not pushed to Git (using updated `.gitignore`).

## Architecture
- **Dependency Management:** No root `package.json` yet. Node.js project initialization recommended.
